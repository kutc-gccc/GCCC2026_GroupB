# Codex作業方針

このプロジェクトでは、Codexの使用量を抑えながら作業する。

- 1回の依頼は可能な限り小さく区切り、長大な会話コンテキストを避ける。
- 検索対象は必要なディレクトリ、ファイル、シンボルに限定する。
- Unityの巨大なシーンファイルや大量のC#コードを、必要なく全文取得しない。
- コマンド出力には行数・件数・文字数の上限を設け、必要部分だけ読む。
- 一度確認した内容をむやみに再検索・再読込せず、得られた情報を利用する。
- 関連する修正は可能な範囲でまとめ、確認と再編集の往復を減らす。
- Web検索やWindows GUI操作は、ローカル情報だけでは判断できない場合に限定する。
- 作業開始時に対象範囲を絞り、安全な仮定で進められる場合は質問の往復を増やさない。
- 検証は変更のリスクに応じた最小限かつ十分な範囲で行う。

ただし、正確性、安全性、ユーザーが明示した要件は使用量削減より優先する。

## Unity実装の共通化方針

- 新しい敵は原則として `SubjiEnemyChase` の共通基盤を使用し、見た目、行動タイプ、速度、索敵、帰還などの個体差は設定値で表現する。
- 敵ごとに追跡、索敵、接触ゲームオーバー、経路移動を複製しない。既存の共通処理で表現できない差分だけを追加する。
- 配置地点へ戻る敵は、開始時の座標を個体ごとに自動保存し、別の場所へ置いても同じスクリプトを再利用できる構造にする。
- 新しいタスクは原則として `JikkenCommentStream` の共通基盤を使用する。
- タスク選択、単一選択制限、タスク欄とミニマップへの表示、達成、コメント色変更、報酬、解除、次IDへの進行をタスクごとに複製しない。
- タスク固有コードは、達成条件、対象、案内文、報酬、次のIDなどの差分に限定する。
- 時間付き操作は `TimedWorldInteraction` を共用し、待機、ゲージ、移動キャンセル処理を複製しない。
- 共通化によって毎フレーム処理や全オブジェクト検索が増えないようにする。イベント、キャッシュ、低頻度判定を優先する。
- 作業開始時は新規クラスを作る前に既存の共通基盤で対応できるか確認し、検索・実装・検証範囲を抑える。
